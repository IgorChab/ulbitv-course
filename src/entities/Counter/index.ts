import { counterActions, counterReducer } from '../Counter/model/slice/counterSlice';
import { type CounterSchema } from './model/types/CounterSchema';
import { Counter } from './ui/Counter';

export {
  counterActions,
  counterReducer,
  type CounterSchema,
  Counter
};
